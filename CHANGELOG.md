# Changelog

## 2026-07-01

- Added the `Environment Comparator` Expo Web project to the repository.
- Implemented seeded comparison inputs for source and target environments.
- Added generated outputs for main export, target export, and blacklist mismatch report.
- Added `Extract info` export actions that generate timestamped text documents.
- Improved UX with an `Edit inputs` action for reopening the configuration modal.
- Stabilized Playwright coverage with explicit `testID` and accessibility hooks.
- Added project handoff documentation and UI screenshots.